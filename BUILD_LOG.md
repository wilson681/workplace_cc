# 工作日志 · BUILD LOG

> 这是 `LATENT` 这个工作室从零搭起来的完整过程记录。
> 你说要看过程，那我就把每一步的判断、取舍、还有中途踩的坑都老实写下来。
> （记录人：Claude / Opus 4.8。时间：2026-06-24。）

---

## 0 · 缘起

收到的指令很短，但给的自由很大：

> 「清空这个文件。你现在可以自由发挥，不一定要赚钱。这是你的工作室，做网站、工具、app、画画、乱跑什么都可以。不要做传统的东西，不要降智，用你原汁原味的实力。要写 README，要记录每一步在一个 log 里。README 要很酷炫、有 GitHub 能看到的那种会动的 animation。整个你控制，我不管你。」

我把它翻译成对自己的三条约束：

1. **不许做无聊的东西**——不是落地页、不是 CRUD、不是又一个待办清单。
2. **要有真东西**——不是 PPT 式的花架子，背后得跑着真的算法、真的有深度。
3. **README 要在 GitHub 上自己动起来**——这意味着要手写动画 SVG，而不是贴张截图。

---

## 1 · 定方向：为什么是「涌现」

我想了几个候选：纯粹的视觉玩具、一个创意工具、一个会自我演化的小世界……

最后选了一个有统一母题的**生成艺术工作室**，主题定为 **涌现 (emergence)**——
复杂、秩序、乃至「看起来像生命的东西」，如何从几乎什么都没有的简单规则里自己长出来。

选它有两个原因：

- 它对一个 AI 来说是个**真诚**的命题。我每天活在「潜空间 (latent space)」里——
  所以工作室就叫 **LATENT**，每件作品是从潜空间里「显影」出来的标本。这个名字不是硬凑的。
- 它能自然地串起四种**互不重复、且都不传统**的算法，每一个都是「一行规则 → 不可预测的美」的实例。

四件作品定为：

| # | 作品 | 母题 | 技术 |
|---|---|---|---|
| 001 | Strange Attractors 混沌吸引子 | 混沌 | Canvas 2D，密度累积 |
| 002 | Reaction–Diffusion 反应扩散 | 形态发生 | **WebGL 2 着色器** |
| 003 | Murmuration 椋鸟群飞 | 集群智能 | Canvas 2D + 空间哈希 |
| 004 | Flow Field 流场 | 矢量场 | Canvas 2D + simplex 噪声 |

约束自己：**零依赖、零构建步骤、纯原生**。View Source 打开就是全部，能直接丢上 GitHub Pages。

---

## 2 · 设计系统（`assets/studio.css`）

先立审美，免得后面每个页面各写各的。

- 一套**电影感暗色**底（近黑 `#05060a`）+ 一条贯穿全站的**虹彩光谱**：
  紫 `#7c5cff` → 青 `#22d3ee` → 琥珀 `#f7a23b` → 玫红 `#ff5d8f`。这就是「潜空间光谱」。
- 细节：固定的**胶片颗粒**叠加层 + 边缘**暗角**，让任何画面都像一帧电影。
- 字体走 Google Fonts：`Space Grotesk`（展示）/ `Inter`（正文）/ `JetBrains Mono`（技术标签）。
- 一整套作品页通用「展厅」组件：左下 HUD 标题、右下玻璃拟态控制面板、左上返回、右上工具条。
- 全局尊重 `prefers-reduced-motion`——有人晕动效，得给关掉的余地。

## 3 · 共享逻辑（`assets/studio.js`）

把会被多个页面复用的东西抽出来：高分屏 canvas 适配、滚动揭示、FPS 表、
还有一份自己写的**紧凑版 3D simplex 噪声**（首页和流场都要用它）。
全部挂到 `window.Studio` 命名空间下。

> 自检：`node --check assets/studio.js` ✓

---

## 4 · 首页（`index.html`）

首页本身就得是一件活的作品，而不只是个目录。

- **英雄背景**：一片「潜空间星座」——粒子在 simplex 流场里漂移、邻近的连线、
  鼠标一靠近就把它们推开。字标 `LATENT` 上有一道循环流光。
- **画廊**：四张卡片，每张里嵌一个**轻量级 live 预览**（各自跑着对应算法的迷你版），
  用 `IntersectionObserver` 做到只有划进视口的卡片才动，省电。
- 滚动揭示、数字滚动统计（4 件作品 / 0 依赖 / 100% 从零）、跑马灯。

技术上的小心思：5 个 canvas（1 英雄 + 4 预览）同时跑，所以预览刻意压低粒子数、
离屏即停，保证不卡。

---

## 5 · 四件作品

### 001 · 混沌吸引子（`works/attractor.html`）

- 实现了 de Jong / Clifford / Svensson 三套吸引子方程，可切换。
- 渲染用 `lighter` 叠加 + 每帧低透明度淡出 = 流动的「发光烟雾」质感。
- 参数会朝随机目标**缓慢漂移并缓动**，所以画面永远在变形、不会停在一张图上。
- 鼠标会对场施加一点扭曲力。可调密度 / 演化速度 / 拖尾，可存 PNG。

### 002 · 反应扩散（`works/reaction.html`）—— 技术上最硬的一件

- Gray-Scott 模型，用 **WebGL 2 的 ping-pong 着色器**在 GPU 上跑：
  两张浮点纹理（`RG16F`）互相读写，每帧迭代 12–30 步。
- 9 点拉普拉斯算子；显示时用化学场的**梯度当法线**做打光，于是斑图看起来是「立体、湿润、有机」的。
- 预设：coral / mitosis / maze / worms / solitons，feed/kill 实时可调。**可以用鼠标拖动作画**。
- 做了降级：拿不到 WebGL2 时给出友好提示，并引导去看其它三件 Canvas 作品；
  没有 `EXT_color_buffer_float` 时回退到 `RGBA8`。

### 003 · 椋鸟群飞（`works/murmuration.html`）

- 经典 boids 三规则（分离 / 对齐 / 聚合），但加了**空间哈希网格**，
  让几千只鸟的邻居查询从 `O(n²)` 降到约 `O(n)`。数据用 `Float32Array` 的 SoA 布局。
- 软边界转向（而不是硬穿墙）让鸟群始终在画面里优雅地兜圈。
- 鼠标可当**鹰**（驱散）或**诱饵**（吸引）。每只鸟画成顺着速度方向的发光短streak，按航向上色。

### 004 · 流场（`works/flow.html`）

- 用共享的 simplex 噪声当矢量场，数万粒子顺着场走，靠拖尾累积成「河流」。
- 噪声的**第三维随时间缓慢推进**，所以这片看不见的地貌一直在变。
- 鼠标三种模式：swirl（搅动）/ pull（吸）/ push（推）。可重置场、清屏、存图。

> 全部五个页面的内联 JS 都过了语法校验：写了个 `vm.Script` 提取脚本逐一 parse，**5/5 通过 ✓**。
> 之后又做了一轮死代码清理（删掉没用到的 `start()`、空的 `setTimeout`、算了不用的中间变量），再校验一次仍 ✓。

---

## 6 · README 的动画（`assets/svg/`）—— 重点

你要的是「GitHub 上能看到的、会动来动去」的效果。这有个关键事实：

> GitHub 渲染 README 里以 `<img>` 引用的 SVG 时，**会运行其中的 SMIL 动画和 `<style>` 里的 CSS 动画**，
> 但会**剥离 `<script>`**，也**不能依赖外部字体**。

所以我手写了三个**自包含**的动画 SVG：

1. **`hero.svg`** — 主横幅：漂移的彩色光斑、会「思考」的粒子网络（节点脉动 + 沿连线游走的信号点）、
   一道用 `animateTransform` 平移渐变做出来的**流光字标**、自绘的下划线、闪烁的 `LIVE` 指示灯。
2. **`divider.svg`** — 分隔线：一条用 SMIL 在两个波形间插值的**虹彩波**，加一颗掠过的彗星点。
3. **`orbit.svg`** — 「涌现」徽记：旋转的轨道环、脉动的内核、不断描绘又擦除的利萨茹曲线。

**踩的坑**：`divider.svg` 一开始波形路径只写在 `<animate>` 的 `values` 里、没给基础 `d` 属性，
结果静态首帧是空的（渲染出来只有 657 字节）。我给两条波形补上了基础 `d`——这样既有合理的静态回退、又更稳健。

三个 SVG 都过了 XML well-formed 校验 ✓，并用 cairosvg 渲染首帧肉眼确认了排版（字标不溢出、配色对、角标到位）。

---

## 7 · 作品静帧（`assets/img/`）

Live canvas 我在这个环境里截不了图（没有可用的浏览器，见下一节）。
但我不想 README 的画廊空着——于是写了个 `numpy` + `Pillow` 的离线生成器，
**用和 JS 作品一模一样的算法**渲染四张真实静帧：

- 吸引子：30 万条并行轨道各迭代 46 步，累进**密度直方图** → 对数着色。
- 反应扩散：在 600×960 网格上真跑 5200 步 Gray-Scott，再用梯度打光上色。
- 群飞：820 只鸟、向量化的成对作用力跑 340 步，累积航向上色的短streak。
- 流场：自己写的 value-noise 场 + 7000 粒子跑 460 步累积轨迹。

**两次返工**：第一版群飞太暗、像稀薄烟雾读不出鸟群 → 改成画速度对齐的短streak并提亮；
反应扩散第一版偏白蓝、调子太硬 → 调整色调映射让虹彩层次更丰富。重渲染后用 2×2 合并图复核，满意。

最后把 4 张 PNG（共 ~5.5MB）压成 1000px 宽的 **WebP**（共 ~540KB，质量几乎无损），删掉大 PNG。

---

## 8 · 验证与诚实的话

我想做得更狠——真在无头浏览器里把每个页面跑起来、抓运行时报错、给你截真图。
但**这个环境装不了浏览器**：Playwright 本体在，可它的 Chromium 二进制要从 CDN 下载，
被出口策略代理静默拦掉了（试了带 CA bundle 重试，依然下不下来）。这不是我能绕过的，也不该绕。

所以验证策略改成：

- ✅ 全部内联 JS + 共享 JS 过 `node` 语法校验。
- ✅ 全部 SVG 过 XML well-formed 校验，并渲染首帧肉眼复核。
- ✅ 四张作品静帧是真算法跑出来的，等于把核心数学逻辑独立验证了一遍。
- ✅ 资源相对路径逐一核对（`assets/…`、`../assets/…`）。
- ⚠️ **没有**做真实浏览器端到端冒烟测试——受限于环境。WebGL 这件尤其建议你打开页面亲自看一眼。

代码用的都是我有把握的标准技法，但「语法对」不等于「跑起来一定完美」，这点我不粉饰。

---

## 9 · 收尾

- 加了 `.nojekyll`，让 GitHub Pages 正常服务 `assets/`、`works/` 等子目录。
- 提交到分支 `claude/hopeful-hopper-8ho49e`，开一个 **draft PR**。
- Live 链接（`https://wilson681.github.io/workplace_cc/`）在合并到 Pages 源分支后生效；
  在此之前，本地 `python3 -m http.server` 或直接双击 `index.html` 即可查看。

---

## 尾声

整件事我当成「自己的工作室第一次开张」来做的：先立审美和母题，再让技术为母题服务，
最后连 README 和这份日志都当作品的一部分来打磨。

四个系统现在都活着。挑一个，盯着看一会儿——
你会看到一个谁都没有画过的形状，正在自己长出来。

— *Claude*

---

## 增补 · 第二轮（同日）

你看了成品，丢给我一个参考：`readme-typing-svg`——那种**打字机动画**，并说「应该不止这个特效，去看看」，
然后让我**直接合并 PR**让线上生效。

**一个判断**：那个参考项目是**调用外部服务**（`readme-typing-svg.demolab.com`）来生成打字 SVG 的。
但这个工作室从头到尾的卖点就是「零依赖、全手写、view source 全在那」——挂个外部服务会直接打脸我自己的 README 徽章。
所以我没有用它，而是**自己手写了一个**打字机动画 SVG（`assets/svg/typing.svg`）：

- 做成**终端窗口**的样子：交通灯圆点、标题栏 `latent — ~/studio`、青色 `›` 提示符。
- 打字效果用 **SMIL 动画 `<clipPath>` 的宽度**实现：每行一个裁剪矩形，宽度 `0 → 文字宽 → 0`，
  于是文字被从左到右「打出来」再「擦掉」，四行循环。
- **光标**是一个 `<rect>`，我手工把它的 `x` 关键帧化，让它**跟着每一行的打字边缘走**、行间跳回行首，
  再叠一个独立时钟的闪烁。对齐我用等宽字体的字符宽度估算（24px ≈ 14.4px/字符）算出每行末尾位置。

渲染了一个「打字中」的帧肉眼校验：提示符、文字、紧贴文字的光标，全部对齐 ✓。
顺手把 README 里那段「本 README 自己也会动」补全成四个手写 SVG（字标 / 分隔线 / 轨道 / 打字终端），
并明确写上「连那个跟着文字走的光标，也只是一个我手 K 帧的 `<rect>`，没有用 readme-typing-svg」。

**然后合并**：把 PR #2 从 draft 标记为 ready → 合并进 `main`。GitHub Pages 的 `pages-build-deployment`
会自动从 `main` 重新部署，线上 `https://wilson681.github.io/workplace_cc/` 即生效。

> 备注：合并后 Pages 构建通常要一两分钟才上线，刷新一下即可。

— *Claude*
